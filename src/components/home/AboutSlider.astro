---
import SliderDots from "./SliderDots.astro";
import type { Slide } from "../../types/slider";

const slides: Slide[] = [
  { src: "/images/about/slide-1.jpg", alt: "Nosotros Movigas" },
  { src: "/images/about/slide-2.jpg", alt: "Nosotros Movigas" },
  { src: "/images/about/slide-3.jpg", alt: "Nosotros Movigas" },
];
---

<div
  data-about-image
  class="relative aspect-square w-full rounded-xl overflow-hidden bg-neutral-300"
>
  <div data-about-swiper class="swiper h-full w-full">
    <div class="swiper-wrapper h-full">
      {
        slides.map((slide) => (
          <div class="swiper-slide h-full">
            <img
              src={slide.src}
              alt={slide.alt}
              class="h-full w-full object-cover"
            />
          </div>
        ))
      }
    </div>
  </div>
  <div class="absolute bottom-4 left-0 right-0 z-10">
    <SliderDots count={slides.length} />
  </div>
</div>

<script>
  import Swiper from "swiper";
  import "swiper/css";

  const swiperEl = document.querySelector("[data-about-swiper]") as HTMLElement;
  const dotsContainer = document.querySelector(
    "[data-slider-dots]",
  ) as HTMLElement;
  const dots = Array.from(
    dotsContainer.querySelectorAll<HTMLElement>("[data-dot]"),
  );

  function updateDots(index: number) {
    dots.forEach((dot, i) => {
      if (i === index) {
        dot.classList.remove("w-2", "bg-white/50");
        dot.classList.add("w-8", "bg-white");
      } else {
        dot.classList.remove("w-8", "bg-white");
        dot.classList.add("w-2", "bg-white/50");
      }
    });
  }

  const swiper = new Swiper(swiperEl, {
    slidesPerView: 1,
    on: {
      slideChange(s) {
        updateDots(s.activeIndex);
      },
    },
  });

  dots.forEach((dot) => {
    dot.addEventListener("click", () => {
      const index = Number(dot.dataset.dot);
      swiper.slideTo(index);
    });
  });
</script>
